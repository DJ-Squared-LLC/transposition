# Copy this file to .env.local and adjust values as needed.
# The React frontend uses VITE_ prefixed variables.

# URL of the Transposition C# .NET API (no trailing slash)
VITE_API_BASE_URL=http://localhost:5000
